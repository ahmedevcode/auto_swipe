import 'package:autoswipe/feature/login/widgets/pin_body.dart';
import 'package:flutter/material.dart';

class pin_view extends StatelessWidget {
  const pin_view({super.key});

  @override
  Widget build(BuildContext context) {
    return
        Scaffold(
          body:pin_body() ,
        );
  }
}
